{
  "name": "irc-client-example",
  "version": "1.0.0",
  "description": "Example showing how to use frida-net's client functionality",
  "private": true,
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile agent -o _agent.js",
    "watch": "frida-compile agent -o _agent.js -w"
  },
  "devDependencies": {
    "frida-compile": "^9.0.6",
    "irc": "^0.5.2"
  }
}
